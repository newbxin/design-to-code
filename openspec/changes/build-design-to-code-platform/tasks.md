## 1. 工程初始化

- [ ] 1.1 在 `client/` 初始化 React + TypeScript + Vite 项目结构。
- [ ] 1.2 在 `client/` 接入 Ant Design、Less、路由、基础布局和 API 请求封装。
- [ ] 1.3 在 `server/` 初始化 Node.js + Express 项目结构。
- [ ] 1.4 在 `server/` 配置静态资源目录、上传目录、临时解压目录和生成资产目录。
- [ ] 1.5 定义前后端共享的数据契约文档或类型，包括 `DesignMetadata`、`GeneratedSchema`、`SchemaNode`、`CodeOptions`、`GeneratedFile`、`ImageCropRequest`。

## 2. 后端视觉稿管理

- [ ] 2.1 实现 `/upload` 文件上传接收和 `.zip` 类型校验。
- [ ] 2.2 实现 zip 解压、临时目录管理和失败清理。
- [ ] 2.3 实现 `index.html` 查找和缺失错误处理。
- [ ] 2.4 实现向 `index.html` 注入 metadata bridge 脚本。
- [ ] 2.5 实现上传成功后的资源目录落盘、`entryUrl` 生成和视觉稿记录创建。
- [ ] 2.6 实现 `/getSketchList`，只返回成功上传的视觉稿记录。
- [ ] 2.7 实现 `/deleteSketch`，删除视觉稿记录并同步删除解压资源目录。
- [ ] 2.8 为上传失败、解压失败、缺少 `index.html`、注入失败和删除失败补充错误响应。

## 3. 后端生成能力

- [ ] 3.1 实现 `/getJsonSchema`，根据 page/block 元数据返回完整 layout schema。
- [ ] 3.2 实现 `/getSkeletonSchema`，根据 page/block 元数据返回保留布局尺寸和层级的 loading skeleton schema。
- [ ] 3.3 实现 schema 节点生成规则，确保节点包含 `id`、`tag`、`type`、`bounds`、`styles`、`children`。
- [ ] 3.4 实现图片类节点的 `asset.sourceImage` 和 `asset.rect` 生成规则。
- [ ] 3.5 实现 `/getImage`，根据 `sourceImage` 和 `rect` 从完整视觉稿图片中裁切资源并返回 URL。
- [ ] 3.6 实现 `/getCode` 的基础 HTML/CSS 代码生成。
- [ ] 3.7 在 `/getCode` 中集成 `/getImage`，为 `img` 和 `background-image` 生成可引用图片 URL。
- [ ] 3.8 实现代码设置对生成结果的基础影响，包括语言、样式类型、单位、导出类型、导出格式和样式引用方式。
- [ ] 3.9 为 metadata 无效、schema 生成失败、代码生成失败、裁切失败补充错误响应。

## 4. 前端视觉稿列表

- [ ] 4.1 实现视觉稿列表页整体布局，与 UI 参考图保持一致。
- [ ] 4.2 实现上传按钮和 zip 上传流程。
- [ ] 4.3 实现上传成功后刷新视觉稿列表。
- [ ] 4.4 实现上传失败错误提示，失败记录不得进入列表。
- [ ] 4.5 实现列表搜索、分页和基础信息展示。
- [ ] 4.6 实现视觉稿编辑入口的基础信息编辑能力。
- [ ] 4.7 实现删除视觉稿交互，并在删除失败时保持列表原状态。

## 5. 前端视觉稿预览与元数据

- [ ] 5.1 实现视觉稿预览页布局和 iframe `entryUrl` 加载。
- [ ] 5.2 实现 iframe 与父页面的 `postMessage` 通信监听。
- [ ] 5.3 实现进入预览页后请求并缓存当前画板 page 元数据。
- [ ] 5.4 实现画板切换时刷新 page 元数据缓存。
- [ ] 5.5 实现点击 block 后设置当前生成目标为 block。
- [ ] 5.6 实现点击空白区域后取消 block 选择并恢复当前生成目标为 page。
- [ ] 5.7 实现布局模式和骨架模式切换。
- [ ] 5.8 实现点击生成时按当前模式调用 `/getJsonSchema` 或 `/getSkeletonSchema`。
- [ ] 5.9 实现点击生成但缺少 page/block 元数据时的错误提示。

## 6. 前端生成效果预览与编辑

- [ ] 6.1 实现生成效果预览页整体布局，包括左侧组件树、中间预览、右侧样式编辑面板。
- [ ] 6.2 实现根据 schema.root 渲染组件树。
- [ ] 6.3 实现根据 schema.root 渲染生成效果预览。
- [ ] 6.4 实现点击预览元素后选中对应 schema 节点并高亮组件树节点。
- [ ] 6.5 实现点击组件树节点后选中对应预览元素并显示红色细虚线边框。
- [ ] 6.6 实现样式编辑面板的字体、布局、定位、背景、边框分组。
- [ ] 6.7 实现样式编辑 patch 到当前前端 schema 的对应 `node.styles`。
- [ ] 6.8 实现未选中节点时样式编辑面板的提示和禁用逻辑。
- [ ] 6.9 实现保存按钮，保存时基于当前 schema 和当前代码设置调用 `/getCode` 并更新生成代码。
- [ ] 6.10 实现左侧组件树和右侧编辑面板的收起/展开交互。

## 7. 前端代码预览

- [ ] 7.1 实现从生成效果预览页进入代码预览页。
- [ ] 7.2 实现只读文件列表和只读代码查看器。
- [ ] 7.3 实现代码设置抽屉。
- [ ] 7.4 实现语言、样式命名、样式类型、单位、导出类型、导出格式、样式引用方式设置项。
- [ ] 7.5 实现设置确认后基于当前 schema 调用 `/getCode` 并更新代码预览。
- [ ] 7.6 确保代码设置只在当前页面会话中生效，刷新后恢复默认设置。
- [ ] 7.7 确保代码内容不可手动编辑。

## 8. 测试与验收

- [ ] 8.1 为 `/upload` 增加合法 zip、非 zip、缺少 `index.html`、注入失败测试。
- [ ] 8.2 为 `/getSketchList` 和 `/deleteSketch` 增加成功和失败路径测试。
- [ ] 8.3 为 `/getJsonSchema` 和 `/getSkeletonSchema` 增加 page/block 输入测试。
- [ ] 8.4 为 `/getImage` 增加有效 rect、无效 rect 和裁切失败测试。
- [ ] 8.5 为 `/getCode` 增加基础代码生成、代码设置变化和图片资源生成测试。
- [ ] 8.6 为前端生成目标切换、metadata 缺失提示、组件树联动、样式 patch 和代码设置重新生成补充测试。
- [ ] 8.7 使用 UI 参考图进行主要页面视觉核对。
- [ ] 8.8 完成端到端手工验收：上传合法 zip、进入预览、选择 page/block、生成布局代码、生成骨架屏代码、编辑样式、保存、查看只读代码。
